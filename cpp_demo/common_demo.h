#ifndef COMMON_DEMO_H // 如果没有定义COMMON_DEMO_H
#define COMMON_DEMO_H // 那么就定义一个COMMON_DEMO_H，应该是为了避免这个.h文件被重复执行

enum  ProjectType // 枚举类，类名的命名规则
{
    Math = 1, // 枚举值
    Chinese = 2,
    English = 3,
};

#endif //需要以endif结尾